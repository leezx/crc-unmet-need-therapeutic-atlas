# Module D question — `tgt_nectin4`

Same method and data sources as `tgt_ceacam5.md`.

> **NECTIN4's RNA signal is already the lowest of the five targets in Module B's screens (both `GSE178318` and `GSE225857`) — does protein-level evidence corroborate that low signal, or contradict it?**

See `data_lock/tgt_nectin4.md`: corroborates. `PXD055821` MS detects `NECTIN4` in only 21.7% of samples — the sparsest of the five targets, consistent with its already-low RNA detection fraction. `HPA_CRC_cancer_tissue` similarly shows mostly `Low`/`Not detected` (8 of 10 patients). Two independent protein-layer sources and two independent RNA-layer cohorts now all point the same direction for this target. ADC precedent for `NECTIN4` is recorded in Module A and not restated here.
