# Module D question — `tgt_nectin4`

Same method and data sources as `tgt_ceacam5.md`.

> **NECTIN4's RNA detection fraction is low relative to the other four targets in Module B's screens (both `GSE178318` and `GSE225857`) — not a calibrated ranking, but the plain fact — does protein-level evidence give this target a limited signal too, or contradict it?**

See `data_lock/tgt_nectin4.md`: gives it a limited signal too, on its own terms. `PXD055821` MS detects `NECTIN4` in 21.7% of specimens. `HPA_CRC_cancer_tissue` similarly shows mostly `Low`/`Not detected` (8 of 10 patients). These are two independent protein-layer reads and, separately, two independent RNA-layer cohorts, each showing a real, target-specific limited-detection pattern for `NECTIN4` — not asserted here as a calibrated cross-target ranking, and not combined into one score across measurement types. ADC precedent for `NECTIN4` is recorded in Module A and not restated here.
