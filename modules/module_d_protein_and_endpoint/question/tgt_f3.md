# Module D question — `tgt_f3`

Same method and data sources as `tgt_ceacam5.md`.

> **F3 has no usable Module E IHC data (a genuine gap, see `modules/module_e_normal_tissue_risk/data_lock/tgt_f3.md`) — does Module D's independent protein-level evidence say anything new?**

See `data_lock/tgt_f3.md`: `PXD055821` MS detects `F3` in only 26.7% of samples (second-sparsest of the five targets), but `HPA_CRC_cancer_tissue` scores every one of 12 patients `Low` (never `Not detected`) — a real, unreconciled cross-source pattern. Neither source closes Module E's normal-tissue IHC gap (that gap is specifically about normal, not tumor, tissue), but this is the first real tumor-tissue protein-level data point this repository has for `F3`. ADC precedent for `F3` is recorded in Module A and not restated here.
