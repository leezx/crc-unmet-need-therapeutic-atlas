# CRC Unmet-Need Therapeutic Atlas — project status

更新时间：2026-08-24（追加：PR #79 已合并）

This file describes current state only. The pre-pivot status (Phase 1 source-only + Phase 2 global fetal-state discovery, through PR #66) is preserved as a historical record at [`../archive/phase2_fetal_state_track_v1/PROJECT_STATUS_LEGACY_2026-08-11.md`](../archive/phase2_fetal_state_track_v1/PROJECT_STATUS_LEGACY_2026-08-11.md) — do not read it as current.

## Current stage: ADC Target Repurposing Atlas pivot (target-first)

Source: `Asset-Generation-OS-architecture.md` → `CRC-Atlas工业化重构`（用户手工备注）, 2026-08-21. This is a **scope pivot**, not a data loss: the repository's single task is now

`ADCdb-derisked target (Module A, gated by `derisking_tier`/`repurposing_status`, external) → mCRC evidence per axis and territory (Modules B–F, `target_id x indication_id`-keyed) → KILL / HOLD / SHORTLIST for that target × territory → supplementary/mechanistic evidence only for a named residual uncertainty on an already-shortlisted target`

— not the old `patient population → malignant cell state → surface target → functional dependency → normal-tissue window` chain, which required an unsupervised cell-state discovery step before a target could even exist. See [`../ADC_ATLAS_DATASET_CONTRACT.md`](../ADC_ATLAS_DATASET_CONTRACT.md), [`../modules/README.md`](../modules/README.md), and [`../knowledge/README.md`](../knowledge/README.md).

PR #70 (`pivot/adc-target-repurposing-atlas`) **merged into `main`** after four review rounds with web-ChatGPT (`CRC临床适应症地图` conversation, `Biotech ideas` project): `REQUEST_CHANGES` on rounds 1–3, `APPROVE` on round 4 (head `b00d7ec`). PR #71 (`phase1-verify-pivot-candidates`, Phase 1 source verification for the 13 pivot-added candidates) **merged into `main`** after three further review rounds: `REQUEST_CHANGES` on rounds 1–2, `APPROVE` on round 3 (head `2a93817`). PR #72 (Module A: `ADC_TARGET_SEED_UNIVERSE.tsv` v1 build) **merged into `main`** after two review rounds: `REQUEST_CHANGES` on round 1, `APPROVE` on round 2 (head `a100198`). PR #73 (first Module B + E vertical slice, `tgt_ceacam5`) **merged into `main`** after three review rounds: `REQUEST_CHANGES` on rounds 1–2, `APPROVE` on round 3 (head `7474004`). PR #74 (`GSE178318` epithelial-proxy screen for `tgt_ceacam5` Module B) **merged into `main`** after two review rounds: `REQUEST_CHANGES` on round 1, `APPROVE` on round 2 (head `2ab59fa` + two small post-approval wording fixes). PR #75 (CNV-lite malignancy-confirmation attempt for `tgt_ceacam5`) **merged into `main`** after three review rounds: `REQUEST_CHANGES` on rounds 1–2, `APPROVE` on round 3 (head `3b9cfde`) — see "CNV-lite malignancy confirmation attempt for tgt_ceacam5" and "Review history" below. PR #76 (`module-b-e-remaining-four-targets`, Module B + E for `ERBB2`/`F3`/`NECTIN4`/`TACSTD2`) **merged into `main`** after three review rounds: `REQUEST_CHANGES` on rounds 1–2, `APPROVE` on round 3 (head `491794f`) — see "Module B + E for the remaining four targets" and "Review history" below. PR #77 (`target-pruning-comparison-five-targets`, horizontal evidence-pattern comparison across all five targets) **merged into `main`** after four review rounds: `REQUEST_CHANGES` on rounds 1–3, `APPROVE` on round 4 (head `8be320b`) — see `reports/TARGET_EVIDENCE_PATTERN_COMPARISON.md` and "Review history" below. PR #78 (`gse225857-cnsa-access-review`, `GSE225857` CNSA access-terms review) **merged into `main`** after three review rounds: `REQUEST_CHANGES` on rounds 1–2, `APPROVE` on round 3 (head `c17a0e3`) — see `DATA/registry/GSE225857/source_manifest.tsv` and "Review history" below. PR #79 (`gse225857-public-metadata-sufficiency-check`, `GSE225857` public GEO metadata sufficiency check) **merged into `main`** after one review round: `APPROVE` on round 1 (head `d31a2ad`, two non-blocking guardrails folded in pre-merge) — see `DATA/registry/GSE225857/file_inventory.tsv` and "Review history" below.

## Current scale

- 32 registry candidates (19 pre-pivot + 13 added this pivot: `GSE274551`, `GSE225857`, `GSE84267`, `PXD055821`, `PXD022613`, `GSE196576`, `GSE294385`, `GSE235919`, `GSE235917`, `GSE5851`, `CSPA_PXD000589`, `CPTAC_COAD`, `HPA_CRC_cancer_tissue`). All still `status=CANDIDATE` — 12 of the 13 have official landing-page source verification (real publication/DOI/PMID, real sample counts where stated) as of 2026-08-21; `CPTAC_COAD` is publication-verified only (`VERIFIED_PUBLICATION_ONLY`) — its Proteomic Data Commons study identity, case count, and file inventory are still unresolved because the PDC portal is a JavaScript SPA that could not be fetched (see "Phase 1 verification pass" below). Verification is not the same as `APPROVED`.
- `DATA/registry/module_classification.tsv`: 34 rows (some datasets serve two modules, e.g. `GSE178318`/`GSE225857` serve both B and C) covering all 32 datasets, each with `module`, `activation_status`, `adc_decision_axis`, `activation_rule`, `activation_context` (added round 3), `default_execution_order` — validated by `scripts/validate_module_classification.py` against a controlled vocabulary, not just free-text `reason`. Module C's axis was `persistence` (round 1) → split into `persistence`/`clinical_endpoint_context` (round 3) → `persistence` retired entirely and split further into `longitudinal_persistence` (a real paired pre/post-treatment design — only `GSE84267` qualifies) and `refractory_or_treated_presence` (single-timepoint presence in treated/refractory tissue — `GSE274551`, `GSE178318`, `GSE225857`, `GSE294385`) (round 4), because a bare `persistence` label was itself a proxy-upgrade risk.
- `datasets.tsv`'s own `priority` column (`P0_DOWNLOAD` / `P1_DOWNLOAD` / `REFERENCE_ONLY`) is retained as **legacy Phase 1 download-priority metadata only**; it is not re-derived from the pivot and must not be read as an execution-priority signal — `module_classification.tsv` is canonical for that (see `CONTRIBUTING.md`).
- `schemas/target_seed.tsv` (round 3: gained `derisking_tier`/`repurposing_status` admission fields) is now populated: `DATA/registry/ADC_TARGET_SEED_UNIVERSE.tsv` has 23 `A_CLINICAL`/`ACTIVE` target rows, validated by `scripts/validate_target_seed.py` (see "Module A seed universe build" below). `schemas/target_evidence.tsv` (round 3: gained `indication_id`, `measurement_layer`, `evidence_directness`, `source_evidence_id`) is still an empty (header-only) contract — no Module B–F finding has been recorded against any of the 23 targets yet. `schemas/evidence.tsv` and `schemas/indication_evidence_links.tsv` gained a `target_id` column (round 2); all 8 existing rows are pre-pivot dataset-provenance evidence and keep `target_id = NA`.
- `config/external_sources.yaml` is new: Module A's two source locations and output contract are declared there via `path_env_var`, not hardcoded into prose docs.
- `archive/phase2_fetal_state_track_v1/` now holds the full old `phase2/` tree, 4 state-validation reports, 7 scripts (all of them — `qc_gse178318.py`, `apply_gse178318_qc.py`, and `audit_hpa_target_window.py` were moved here in round 2 after review caught that they still defaulted to the old Fig1 marker panel), and the pre-pivot `PROJECT_STATUS.md` body.

## Completed this pivot (rounds 1–4)

1. `ADC_ATLAS_DATASET_CONTRACT.md` — per-module allowed questions / forbidden claims / activation rules; round 3 added the Module A admission gate and the two-table evidence model description.
2. `modules/` — one working folder per Module B–F; admission language corrected to match canonical registry `status` (round 2); Module C's `persistence` vs `clinical_endpoint_context` split and Module B's surface-density boundary hardened (round 3).
3. `DATA/registry/module_classification.tsv` + `schemas/module_classification.tsv` + `scripts/validate_module_classification.py` — canonical execution-priority contract (round 2), extended round 3 with `activation_context` (names the actual RAS-mutant/RAS-WT/anti-EGFR/MRD/first-line territory instead of leaving it in free-text `reason`) and the `clinical_endpoint_context` axis.
4. `schemas/target_seed.tsv`, `schemas/target_evidence.tsv`, `target_id` on `schemas/evidence.tsv` / `schemas/indication_evidence_links.tsv`, `config/external_sources.yaml` (round 2) — round 3 makes the two-table model explicit (`evidence.tsv` = provenance object, `target_evidence.tsv` = canonical `target_id x indication_id`-keyed interpreted output, linked via `source_evidence_id`) and adds the Module A `derisking_tier`/`repurposing_status` admission gate.
5. Old fetal-state track fully archived: `phase2/`, its 4 reports, and all 7 of its scripts (round 2).
6. `.gitignore` restores the original `phase2/03_data/raw|processed|06_results` patterns in addition to the new archive paths (round 2).
7. `knowledge/README.md` rewritten target-first; the old mandatory malignant-cell-state evidence chain is retracted (round 2), then updated for the two-table model and `indication_id`-keyed dossiers (round 3).
8. Consistency fixes (round 3): `CSPA_PXD000589`'s `datasets.tsv` reason text now says Module F, not Module A/B; this file's candidate-count arithmetic corrected to 19+13; `config/project_completion.yaml`'s remaining-component name corrected to thirteen; Module D README's HPA_CRC_cancer_tissue file-inventory claim removed (it has none yet — only `MCRC_liver_metastasis_PDO_2026` does).
9. Module C's `persistence` axis retired and split into `longitudinal_persistence` (only `GSE84267` — the one genuine paired pre/post-treatment design) and `refractory_or_treated_presence` (`GSE274551`, `GSE178318`, `GSE225857`, `GSE294385` — single-timepoint presence in treated/refractory/metastatic tissue, which proves presence, not treatment-induced retention) (round 4). Module A's `B_PRECLINICAL_ADC` tier tightened to require a real preclinical ADC construct (in-vitro killing / in-vivo efficacy), not just antibody-internalization evidence — that alone now falls to `C_ANTIBODY_OR_BIOLOGY_ONLY` → `FUTURE` by default (round 4). "Next handoff" wording corrected: Modules B+E are the intentionally selected minimum first-pass slice, not "the two `CORE_ACTIVE`/`every_target` modules" — C and D also have `CORE_ACTIVE`/`every_target` rows (round 4).

## Current gates and limits

- **Phase 1 source verification for 12 of the 13 pivot-added candidates is done as of 2026-08-21** (see "Phase 1 verification pass" below and `reports/P0_SOURCE_VERIFICATION.md`/`.tsv`). Verification here means: real publication/DOI/PMID recorded, **official repository landing page/accession independently confirmed**, real sample/patient counts filled in where the source states them, `UNKNOWN` preserved where it genuinely doesn't. `CPTAC_COAD` does not meet that bar yet — only its landmark publication is confirmed; its actual PDC repository identity is deferred, not verified, and it is recorded as such rather than being folded into the "13/13 complete" claim. It does **not** mean any candidate is `APPROVED` — none are, matching every other candidate in this registry.
- Several fields remain genuinely `UNKNOWN` even after verification (e.g. exact patient counts for some GEO series, most treatment/molecular annotations, file-level inventories for the two PXD proteomics candidates and CPTAC_COAD) — that is the correct, honest output, not a gap to hide.
- No Module (B–F) has a data lock or analysis contract yet — `modules/*/README.md` define scope only.
- `MCRC_liver_metastasis_PDO_2026`'s mIHC panel covers only 14 markers; Module D must do a `target_observable` coverage check per target before assuming RNA→protein calibration is available.
- `knowledge/` still has only the 8 pre-pivot, non-target-specific evidence objects; no `target_evidence.tsv` row exists yet for any real ADCdb target.
- Module A's v1 `ADC_TARGET_SEED_UNIVERSE.tsv` covers only the 23 targets behind an already-approved-or-clinical-stage ADC construct (`adc_candidates.tsv`, `status=VALIDATED`); it does **not** enumerate ADCdb's broader ~300-antigen universe (`B_PRECLINICAL_ADC` / `C_ANTIBODY_OR_BIOLOGY_ONLY` tier candidates that would default to `FUTURE`) — extending coverage there is future work, not started.

## Phase 1 verification pass (2026-08-21)

All 13 pivot-added candidates were checked against their official landing page (GEO / ProteomeXchange / HPA) and, where linked, their associated publication (Europe PMC); 12 reached a real landing-page-verified state, `CPTAC_COAD` reached publication-only (see above). Real facts replaced placeholder `UNKNOWN`s in `datasets.tsv` and each candidate's `source_manifest.tsv`; two candidates (`GSE274551`, `GSE235919`) have a single named processed file and now carry a real `file_inventory.tsv` instead of a disposition placeholder; most of the rest keep `no_file_inventory_disposition.tsv`, rewritten with a dataset-specific reason (usually: a bundled `RAW.tar` or an unqueried API listing) instead of generic text.

The pass surfaced newly-discovered facts that required correcting the *canonical* dataset role, not just filling in metadata (PR #71 review caught that a first commit had recorded some of these facts without propagating them into `module_classification.tsv`/`datasets.tsv`'s other fields — fixed in the same PR):

- `GSE225857`'s GEO series formally lists only 8 samples, but its publication (Science Advances 2023, PMID 37327339) confirms the architecture doc's "27 samples / 6 patients" figure is correct at the publication level — most raw single-cell data is hosted at CNSA (`CNP0002540`, `CNP0003321`), not GEO, for patient-privacy reasons. Its `treatment_annotation` is `CHEMOTHERAPY_AND_OR_RT_PREOPERATIVE`, sourced directly to GEO's own "Overall design" field ("All the patients received preoperative chemotherapy and/or radiotherapy").
- `GSE235917` was reclassified out of Module C entirely: its own verified `source_manifest.tsv` and the official GEO record both show it is PBMC/tumor-infiltrating-lymphocyte scRNA-seq (immune cells) — it cannot support the malignant-epithelial tumor-cell-localization claim it was previously registered for. Now `SUPPLEMENT_FROZEN`, activated only for a target-specific immune/TME liability question.
- `GSE196576`'s `n_samples` is the source count (579), not the 469-profile analytic subset (mixed primary/metastatic/unknown-origin, not primary-only) that had been conflated into the same field.
- `GSE5851`'s recorded publication was a GEO-series-style placeholder title/2008; corrected to the real peer-reviewed publication (J Clin Oncol 2007, PMID 17664471, DOI 10.1200/jco.2006.10.5437).

Full detail: `reports/P0_SOURCE_VERIFICATION.md` / `.tsv`.

## Module A seed universe build (2026-08-23)

`DATA/registry/ADC_TARGET_SEED_UNIVERSE.tsv` was generated by the new `scripts/build_target_seed_universe.py`, which resolves both Module A source locations from `config/external_sources.yaml` via their `path_env_var` (fails closed if unset — never falls back to `example_local_path`) and joins:

- `$ADCDB_CLAUDE_REDO_PATH/DATA/feasibility/adc_candidates.tsv` — 30 curated, `status=VALIDATED` known-ADC-asset rows (14 approved, 16 clinical-stage).
- `DATA/reference/adcdb_asset_antigen_crossref.tsv` (new, checked in) — each asset's antigen manually cross-referenced against `$ADCDB_PUBLISHED_PATH/ADCs/*.md`'s own `antigen:` wikilink field (source-verified per asset, not inferred from drug-name conventions), with an explicit `resolution_status` per row: 27 `RESOLVED_DIRECT` (a dedicated `ADCs/<asset>.md` file's own `antigen:` field), 2 `RESOLVED_BACKLINK` (`sigvotatug vedotin`, `bulumtatug fuvedotin` — no dedicated ADC file, resolved via the antigen file's own "Related ADCs" backlink instead), and 1 `UNRESOLVED_SOURCE_GAP` (`glembatumumab vedotin` — its ADC detail file is linked from `ADC.index.md` but missing from the local vault; no target was assigned rather than guessing one from memory). `scripts/build_target_seed_universe.py` (round 2 of PR #72) hard-fails if this file's `asset_entity_id` set drifts out of sync with `adc_candidates.tsv`'s `status=VALIDATED` set, or if a `RESOLVED_*` row's antigen file goes missing — a partial/stale seed universe must not be emitted silently.
- `$ADCDB_PUBLISHED_PATH/Antigens/<name>.md` — each antigen's own General Information table (`Gene Name`, `HGNC ID`, `Antigen ID`).
- `DATA/reference/uniprot_accession_map.tsv` (new, checked in) — UniProt accession per gene symbol, retrieved from the UniProt REST API and cross-checked against each antigen file's own `Uniprot Entry` mnemonic (all 23 matched exactly).

Result: **23 unique targets**, all `derisking_tier=A_CLINICAL` / `repurposing_status=ACTIVE` per the contract's own admission-gate definition (a real ADC construct against each has entered a human trial or is approved, per `adc_candidates.tsv`). `human_adc_exposure_evidence` is a deliberately separate, narrower claim: only `YES` when an *approved* asset exists for that target (approval implies documented dosing); a target with only clinical-stage (Phase 1–3) assets gets `UNKNOWN` there even though its `derisking_tier` is still `A_CLINICAL` — a registered trial is not the same evidence as documented human exposure, and `adc_candidates.tsv` has no per-asset dosing-evidence field to draw on for the clinical-stage-only case. `adcdb_cancer_types_with_precedent` is a distilled, deduplicated list of each target's documented trial/label indications (colorectal/colon/rectal-relevant terms are surfaced first, capped at the same total limit as everything else, and a term with explicit negative/exclusion context, e.g. "Excluding ... Colorectal Cancer", is never counted as precedent — see `scripts/test_build_target_seed_universe.py`). **Five targets have real, source-recorded CRC/mCRC precedent**: `CEACAM5` (Labetuzumab govitecan), `ERBB2`/HER2 (Trastuzumab emtansine/deruxtecan, Disitamab vedotin), `F3`/Tissue Factor (Tisotumab vedotin), `NECTIN4` (Enfortumab vedotin), `TACSTD2`/Trop-2 (Sacituzumab govitecan, Datopotamab deruxtecan) — none of this is a CRC efficacy or safety claim, only documented trial/label indication text; whether any of these five actually clears Modules B–F for a specific `indication_id` is unknown and untested.

**Scope boundary, stated explicitly, not hidden**: this v1 build only covers targets behind an already-clinical-or-approved ADC construct (`adc_candidates.tsv`'s curated 30-asset registry). ADCdb's broader local snapshot (`ADCDB_PUBLISHED_PATH/Antigens/`) lists ~300 additional antigens that would need per-target preclinical-evidence review to classify as `B_PRECLINICAL_ADC` vs `C_ANTIBODY_OR_BIOLOGY_ONLY` — that classification pass has not been started and is not part of this build.

**PR #72 round 1** (web-ChatGPT, `REQUEST_CHANGES`) caught that the first build was not actually fail-closed for a canonical generated artifact: the generator hardcoded the two env var names instead of reading them from `config/external_sources.yaml`, silently warned-and-skipped instead of hard-failing on a missing/extra crossref row or a vanished antigen file, unconditionally set `human_adc_exposure_evidence=YES` (upgrading "clinical stage" into "documented human exposure"), and the `adcdb_cancer_types_with_precedent` distillation had three live bugs — a negative-context indication string ("... Excluding ... Colorectal Cancer") counted as positive CRC precedent, `split(";")` shredding a cytogenetic-notation label (`t(9;22)(q34.1;q11.2)`) into a garbage fragment, and the CRC-priority cap not actually bounding total shown terms. All four fixed in round 2: the generator now reads `path_env_var` names from the YAML, reconciles the crossref file's `asset_entity_id` set against `adc_candidates.tsv`'s `status=VALIDATED` set and hard-fails on any drift (see `DATA/reference/adcdb_asset_antigen_crossref.tsv`'s new `resolution_status` column above), `human_adc_exposure_evidence` is `YES` only for targets with an approved asset, and the distillation logic excludes negative-context terms, splits on `;` only outside parentheses, and enforces `cap` as a true total — covered by new `scripts/test_build_target_seed_universe.py` regression tests. Output content is unchanged (still 23 targets) except for the corrected `human_adc_exposure_evidence` values and cleaner `adcdb_cancer_types_with_precedent` text.

Validated by `python3 scripts/validate_target_seed.py` against `schemas/target_seed.tsv` (header match, `target_id`/`target_symbol` uniqueness, `tgt_<symbol>` naming convention, controlled vocabulary, and the tier→status default-rule check) and `python3 scripts/test_build_target_seed_universe.py` (distillation regression tests).

## Module B + E first vertical slice (2026-08-23)

First real attempt at "take one `target_id x indication_id` through Modules B and E end to end," per PR #72's own next-handoff and web-ChatGPT's explicit reviewer note. Picked `tgt_ceacam5` (`CEACAM5`) — the most CRC-specific of the five CRC-precedented `ADC_TARGET_SEED_UNIVERSE.tsv` targets (Labetuzumab govitecan is CEACAM5-directed with CRC-specific indication text, unlike the pan-tumor precedent of `ERBB2`/`F3`/`NECTIN4`/`TACSTD2`).

**PR #73 round 1** (web-ChatGPT, `REQUEST_CHANGES`) caught four real problems with the first pass, all fixed the same round: (1) `TE001`/`TE002` used `indication_id=normal_tissue_safety_reference` while Module B targeted `mcrc_preop_chemotherapy_crlm`, silently splitting one target into two dossiers — all three rows now use `mcrc_preop_chemotherapy_crlm`; (2) the HPA RNA file was mislabeled "consensus" (HPA's separate 51-tissue product) when it is actually the "RNA expression (HPA)" 40-tissue product, and — more importantly — the data lock claimed HPA IHC was unavailable when this repository's own `DATA/registry/HPA_normal_tissue/source_manifest.tsv` already recorded it as downloaded with a SHA256; the IHC file is in fact present (gitignored, at `archive/phase2_fetal_state_track_v1/phase2/03_data/raw/HPA_normal_tissue/`) and is now used for real — GTEx's own registry entry was also backfilled with the exact file/checksum this run used, which it hadn't been; (3) two claims had drifted into a proxy upgrade ("confines... to GI-mucosal tissue" when the same row reported a non-trivial lung value; "signal tracks epithelial content specifically" from bulk RNA alone) — both reworded to what the evidence actually supports; (4) Module B's data lock claimed no processed data existed for `GSE178318`, contradicting this repository's own `source_manifest.tsv`, which records its matrix/genes/barcodes as downloaded and SHA256-verified, physically present at the same gitignored path — corrected to say the real blocker is malignant/epithelial cell-type annotation, not data access (`GSE225857` genuinely has no local data, unlike `GSE178318`). The indication ontology's `mcrc_preop_chemotherapy_crlm.prior_therapy` was also loosened from a specific regimen (`CAPEOX_OR_FOLFOX_BEV`) to `PREOPERATIVE_CHEMOTHERAPY_AND_OR_RT`, matching what the two source datasets actually, verifiably support.

**Module E: real, corrected.** `scripts/extract_normal_tissue_rna.py` now reads HPA's RNA and IHC files from this repository's own checksum-verified local cache (not an external `path_env_var` resource — that was wrong in round 1), and GTEx v11 median TPM externally via `config/external_sources.yaml`'s `module_e_gtex_bulk_rna_reference` block. Result across all three layers: CEACAM5 is predominantly enriched in GI tissue at the RNA level (colon 920.3, rectum 885.1, appendix 175.2, stomach 154.1, esophagus 148.8 HPA nTPM), and the cell-type-resolved HPA IHC layer (`TE003`, newly added) sharpens this — High only in Appendix/Colon/Rectum enterocytes/endocrine/goblet cells, Medium only in a few GI-adjacent cell types, and Low in lung alveolar cells type I/II and bone marrow hematopoietic cells. This does not corroborate the RNA-only lung/tonsil/smooth-muscle flag from round 1 at High/Medium protein level, which lowers — but does not eliminate — that concern (round 2 review: HPA's cell-type panel is not exhaustive, so "does not corroborate" is the correct claim, not "resolves"). Three real `target_evidence.tsv` rows (`TE001`-`TE003`), all `evidence_directness=UNCALIBRATED_PROXY` (round 2 review: IHC is not itself a calibration step — no target-specific quantitative surface assay backs it, so `TE003` does not earn `CALIBRATED_PROXY` even though `measurement_layer=IHC` distinguishes it from RNA), backed by three `evidence.tsv` rows (`EV009`-`EV011`). Full detail: `modules/module_e_normal_tissue_risk/question|data_lock|analysis_contracts/tgt_ceacam5.md` (full per-tissue result TSVs are gitignored, not committed — regenerable).

**Module B: honestly blocked, correctly described this time.** `GSE178318`'s processed matrix, gene index (`CEACAM5` confirmed present) and patient/sample-level treatment annotation (`sample_map.tsv`) are all locally present and SHA256-verified — the real blocker is that malignant/epithelial cell-type annotation was never done for any target, not a missing file. `GSE225857` genuinely has no local processed data (CNSA access terms unreviewed). Both remain `status=CANDIDATE` in `datasets.tsv`; no `target_evidence.tsv` row was written for Module B. Full detail: `modules/module_b_mcrc_target_prevalence/question|data_lock/tgt_ceacam5.md`.

New `scripts/validate_target_evidence.py` validates `target_evidence.tsv`'s header, `target_evidence_id` uniqueness, and every cross-reference (`target_id` against `ADC_TARGET_SEED_UNIVERSE.tsv`, `indication_id` against `clinical_indications.tsv`, `module`/`adc_decision_axis`/`evidence_directness` controlled vocabularies, `dataset_id`/`source_evidence_id` against their own registries).

## `GSE178318` epithelial-proxy screen for `tgt_ceacam5` (2026-08-23)

Per PR #73 round 3's own reviewer recommendation: attempt `GSE178318`'s malignant/epithelial cell-type annotation before running Module E on the other four CRC-precedented targets. This did **not** fully close Module B for this target — round 1 of PR #74's review independently fetched `GSE178318`'s own publication (Cell Discovery 2021, DOI `10.1038/s41421-021-00312-y`, corrected 2026-08-23 from a non-existent DOI previously recorded in `DATA/registry/GSE178318/source_manifest.tsv`) and found the first attempt fell below that paper's own published standard: no QC filtering, a scoring bug that structurally favored marker categories with more genes, an epithelial marker panel that risked misclassifying normal hepatic epithelium in liver-metastasis samples, and treatment-naive patients folded into a treatment-defined `indication_id` with only a notes-field caveat. `scripts/annotate_gse178318_cell_types.py` was corrected: real QC (a paper-aligned operationalization of the source publication's own criteria — >=500 detected genes and <=15% mitochondrial UMI are explicit in the paper; per-sample 3-SD outlier removal on log-total-UMI is the paper's own stated rule, extended to gene-count outliers as the most direct reading of its less-specific "unusually high or low" wording; 123,330 of 140,281 barcodes pass), EPCAM-alone epithelial identification (matching the paper's method, not a multi-gene panel), a fixed marker-average scoring formula, and separate reporting for the 3 chemo-treated (`COL15`/`COL17`/`COL18`) vs. 3 treatment-naive (`COL07`/`COL12`/`COL16`) patients under different `indication_id`s.

**This remains an epithelial-proxy screen, not a malignancy-confirmed result** — the source publication itself confirms malignancy via InferCNV (transcriptome-inferred copy-number variation), which is not reproduced here (would need a genomic gene-position reference and a chosen normal-cell reference population). Module B's actual malignant-cell prevalence question for `tgt_ceacam5` remains **open**. Result: treated patients (`indication_id=mcrc_preop_chemotherapy_crlm`, `TE004`) show CEACAM5-positive epithelial-proxy fractions of 12-60%, with epithelial-proxy cell counts low and concentrated in `COL15` — directionally consistent with the paper's own report of few EPCs in treated patients. Treatment-naive patients (`indication_id=mcrc_liver_metastasis`, `TE005`) are kept as separate context evidence. `GSE225857` remains genuinely blocked on CNSA access. Full detail: `modules/module_b_mcrc_target_prevalence/question|data_lock|analysis_contracts/tgt_ceacam5.md` (full per-sample result TSV is gitignored, not committed — regenerable).

`tgt_ceacam5` now has 5 `target_evidence.tsv` rows total (`TE001`-`TE005`) spanning Modules A (seed universe), B (epithelial-proxy prevalence, not malignancy-confirmed), and E (normal-tissue risk) — real progress, but the vertical slice is not fully closed until malignancy is actually confirmed for the Module B half.

## CNV-lite malignancy confirmation attempt for `tgt_ceacam5` (2026-08-23, round 3 re-run — cohort-aligned)

Direct attempt to close the gap the previous section leaves open. New `scripts/infercnv_lite_gse178318.py` builds a **chromosome-arm-level CNV score** — a coarser, independently-designed approximation of `GSE178318`'s own fine-grained, per-gene-windowed InferCNV, not a reproduction of it. Gene-to-chromosome-arm mapping comes from this machine's local HGNC gene-position reference (`config/external_sources.yaml`'s `module_b_gene_position_reference` block, `path_env_var` actually parsed out of that YAML — round-1 review caught a hardcoded copy — and checksum-locked via `DATA/reference/hgnc_gene_id_mapping_source_lock.tsv`; 25,376 of 33,694 genes resolve to an arm, 43 arms with >=10 genes). Reference/normal population: QC-passing, immune-classified cells within `PRIMARY_CRC`/`LIVER_METASTASIS` samples only (not `PBMC`), split into a fit half (defines per-arm mean/SD) and a held-out half (its own 99th percentile sets the `CNV_HIGH` threshold). All "reference" comparison numbers use the held-out half consistently (round-1 review fix).

**Round-2 review caught a cohort-mismatch bug**: despite the analysis contract always specifying the 3 treated patients (`COL15`/`COL17`/`COL18`) as scope, the reference and epithelial-proxy populations were not actually restricted to them — both silently pooled all 6 `GSE178318` patients, and those pooled numbers (82% treatment-naive cells) were reported as `TE006`/`EV014`'s canonical statistic under the treated-cohort `indication_id=mcrc_preop_chemotherapy_crlm` — the same class of bug PR #74 round 1 caught for the epithelial-proxy screen itself. Fixed by splitting the script into a canonical treated-only run and a separate pooled six-patient diagnostic (kept only as a method sanity check, never as the canonical statistic).

**Honest result (treated-only, canonical): ambiguous, not confirmatory.** Epithelial-proxy cells (n=1,786, `COL15`/`COL17`/`COL18` only) show a modest median CNV-score shift over the held-out treated-cohort reference (n=19,002; 1.32 vs. 0.63, ~2.1x) — directionally consistent with malignant signal — but the two distributions overlap substantially: 36/1,786 (2.02%) of epithelial-proxy cells clear the held-out threshold vs. 190/19,002 (1.00%) of the held-out reference's own cells over that same threshold (enrichment ratio 2.02x — a modest enrichment, somewhat clearer than the non-canonical pooled diagnostic's 1.23x, but still far from a sharp separation; the reference's own ~1% rate is the null expectation by construction of a 99th-percentile threshold, not itself a finding). Per-sample `CNV_HIGH` counts are mostly single-digit (0-22 cells across the 6 treated samples) to support a reliable per-sample CEACAM5 prevalence estimate. This coarse arm-level method is missing within-cell gene-order local structure and moving-window smoothing and a centering/reference-subtraction step relative to real InferCNV workflows (not primarily "no cross-cell smoothing" — cross-cell clustering/HMM subclustering is an optional InferCNV refinement, not its core distinguishing mechanism); its `mean(z^2)` aggregate also erases gain/loss direction. A lineage-driven expression-breadth confound (epithelial vs. immune, unrelated to copy number) is a co-equal, un-excludable alternative explanation for the median shift, not a secondary caveat to the resolution-loss explanation. One `target_evidence.tsv` row (`TE006`, `evidence_level=EXPLORATORY_UNDERPOWERED`, `evidence_directness=UNCALIBRATED_PROXY`), backed by `EV014`. **Does not supersede `TE004`/`TE005`** — Module B's malignant-cell prevalence question for `tgt_ceacam5` remains genuinely open, now for a specific, characterized reason. Full method and results: `modules/module_b_mcrc_target_prevalence/analysis_contracts/infercnv_lite_gse178318.md`.

## Module B + E for the remaining four targets (2026-08-24)

Explicit user instruction: run Modules B + E for the four other CRC-precedented targets (`ERBB2`, `F3`, `NECTIN4`, `TACSTD2`), prioritizing breadth across all five targets over further depth on `tgt_ceacam5`'s malignancy-confirmation question — this supersedes the prior round's "do not batch-run" recommendation (below, item 2 as originally written), which was itself only a recommendation pending a scope decision, not a hard constraint.

Same corrected method as `tgt_ceacam5` applied directly, first pass for the raw data pipeline itself (lessons from PR #73/#74 already incorporated for QC/scoring/cohort-splitting): `scripts/extract_normal_tissue_rna.py`/`scripts/annotate_gse178318_cell_types.py --gene <SYMBOL>` re-run for each target against the same already-locked, checksum-verified local sources (no new downloads). 20 new `target_evidence.tsv` rows (`TE007`-`TE026`, 5 per target), 20 new `evidence.tsv` rows (`EV015`-`EV034`) — 19 `evidence_directness=UNCALIBRATED_PROXY`, one `UNKNOWN` (`TE014`, `F3`'s IHC row — no usable IHC data exists, fixed 2026-08-24 in round 1 review, below, since "no evidence" must not be coded as a weak proxy). No CNV-based malignancy confirmation attempted for any of the four — all four remain epithelial-proxy screens, not malignancy-confirmed, per the `tgt_ceacam5` CNV-lite reviewer's own recommendation not to repeat that method per-target.

Real protein-level findings, reported as HPA/GTEx expression only, with **no causal clinical-toxicity claim** (round 1 review caught several unsupported "matches known ADC toxicity" claims here, one of them factually wrong — fixed, see "Review history" below): `ERBB2`'s HPA IHC scores cardiomyocytes and lung alveolar cells Medium; `NECTIN4` shows Medium skin IHC on top of high skin RNA; `TACSTD2` reaches IHC-High in skin — one of two targets in this repository with any IHC-High row, alongside `CEACAM5`'s 12 from PR #73 (not "the only High-level rows across all five targets," an earlier round-1 error) — but also has broad non-colorectal epithelial expression beyond skin (esophagus RNA 365.6 is even higher than skin's 225.6). Real, notable gaps: `F3` has no usable HPA normal-tissue IHC data (one row, `Reliability=Uncertain`) — its Module E assessment rests on bulk RNA alone; `NECTIN4`/`TACSTD2`'s low-signal compartments are colon/rectum/small-intestine specifically, not GI broadly (both show real Medium IHC in esophagus, upper GI — round 1 caught an internal contradiction where the same row claimed "no GI tissue scored High/Medium" while listing esophagus as Medium); `GSE178318`'s own gene index carries `NECTIN4` under its prior HGNC symbol `PVRL4`, not the current symbol (`--gene PVRL4` was the actual Module B invocation, results reported under `NECTIN4` throughout — see `modules/module_b_mcrc_target_prevalence/data_lock/tgt_nectin4.md`). `GSE225857` remains blocked for all five targets, unchanged.

## Next handoff

0. ~~PR #76 round-3 reviewer's explicit recommendation: horizontal comparison across the five CRC-precedented targets~~ — done 2026-08-24, PR #77 (`reports/TARGET_EVIDENCE_PATTERN_COMPARISON.md`, merged after four review rounds — see "Review history" below). Reframed during review as **next-uncertainty prioritization**, not target pruning, after catching that an early draft had treated Module B's per-gene RNA detection fractions and Module E's per-target IHC categorical levels as calibrated cross-target scales (neither is licensed by this evidence), and a conceptual error treating `CEACAM5`'s colorectal-concentrated normal-tissue signal as resolving normal-tissue risk (it does not). Final state: `CEACAM5`'s next-most-valuable evidence is an independent cohort or real protein/surface data; `NECTIN4`'s is verifying whether its low RNA detection fraction replicates; `ERBB2`/`TACSTD2` (tissue-breadth footprint 13 and 9 distinct HPA tissues respectively, the two broadest of the five) need protein/surface-density evidence before their normal-tissue pattern can be read as risk; `F3`'s normal-tissue axis (no usable IHC) is a genuine gap whose priority depends on other evidence. Not a decision this repository makes unilaterally — no next step from this list has been started.
1. Module B's malignant-cell prevalence question for `tgt_ceacam5` is still open after two real attempts (epithelial-proxy screen, CNV-lite confirmation attempt). A materially higher-power method (real gene-window InferCNV with within-cell smoothing and a centering/reference-subtraction pipeline, or an actual InferCNV package if one becomes available in this environment) would be the next step if this specific question is worth pursuing further — not started.
2. ~~Do not batch-run the other four CRC-precedented targets~~ — done 2026-08-24 per explicit user direction (see "Module B + E for the remaining four targets" above); all five `A_CLINICAL` targets now have real Module B + E evidence. None has attempted CNV-based malignancy confirmation except `tgt_ceacam5` — if a higher-power malignancy-confirmation method is eventually built, it is still a per-target decision, not yet warranted to repeat across all five.
3. ~~`GSE225857`'s CNSA access terms need independent review~~ — done 2026-08-24, PR #78 (`DATA/registry/GSE225857/source_manifest.tsv`, merged after three review rounds — see "Review history" below). The CNSA raw sequencing-data route is confirmed a real, not merely unreviewed, blocker: both accessions (`CNP0002540` scRNA-seq, `CNP0003321` spatial, split into separate provenance rows) are `CONTROLLED_ACCESS`, requiring a Data Access Center (DAC) application approved solely at the discretion of the original submitter (Wang Fei, Guangdong Academy of Medical Sciences, no stated turnaround time) plus China Human Genetic Resources (HGR) regulatory compliance — applicant eligibility and exact institutional/HGR filing obligations are not resolved by this repository review. **This does not, by itself, mean extending Module B requires a DAC application**: `GSE225857`'s own public GEO record already provides downloadable *processed* count matrices (raw UMI counts, per GEO's own field name — GEO's own record states raw sequencing data is not provided here, only processed data) + per-cell metadata for the pooled immune (`GSM7058754`) and non-immune (`GSM7058755`) cell populations, plus per-sample spatial matrices for 6 tumor samples (`GSM7058756`-`761`) — a public processed-data route, distinct from the CNSA raw-sequencing-data route.
3a. ~~Open the two public `GSM7058754_immune_meta.txt.gz`/`GSM7058755_non_immune_meta.txt.gz` files and check whether their columns carry patient/sample/site/treatment labels~~ — done 2026-08-24 (PR #79, `DATA/registry/GSE225857/file_inventory.tsv`). Both files were actually downloaded (real SHA256 checksums, gzip-integrity-verified), and **the answer is yes**: both carry a `patients` column (7 distinct IDs), an `organs`/`samples` column (5 site codes in the immune-fraction file, 2 in the non-immune fraction file), and a combined `patients_organ` column — real patient- and anatomical-site-level resolution, cross-checked against the source publication's own methods text (Wang et al., Sci Adv 2023, PMID 37327339: primary colorectal cancer/adjacent normal colon/liver metastasis/adjacent normal liver/peripheral blood, CD45+ vs CD45- sorted fractions) and matching its reported cell counts exactly (196,473 immune + 41,892 non-immune). One honest discrepancy, not investigated further: the metadata carries 7 distinct patient IDs (`s0107`/`s0115`/`s0813`/`s0816`/`s0920`/`s1125`/`s1231`) against the publication's stated "six patients" — `s0816` may be a QC-excluded patient retained in the raw GEO deposit, not confirmed. No per-cell treatment column exists, but none is needed: the registry's existing series-level `treatment_annotation` (`CHEMOTHERAPY_AND_OR_RT_PREOPERATIVE`) already applies uniformly to this cohort, unlike `GSE178318` which needed a treated/treatment-naive split. **This confirms the public GEO route is structurally sufficient for a Module B prevalence read and the CNSA DAC route now looks unnecessary for this purpose** — but this check only opened the metadata files, not the much larger count matrices (`GSM7058754_immune_counts.txt.gz` 213.9 MB, `GSM7058755_non_immune_counts.txt.gz` 86.2 MB), so target-gene detectability is still unconfirmed. **Next concrete step, not started** (narrowed per PR #79's own reviewer): prioritize downloading only `GSM7058755_non_immune_counts.txt.gz` (86.2 MB) first, not both count matrices — Module B cares about tumor-target prevalence, and the CD45- non-immune fraction already has primary/liver-metastasis site resolution; the 213.9 MB immune matrix is not obviously needed yet. Before rebuilding a fresh EPCAM-only proxy (the method `GSE178318`'s screen had to use for lack of an alternative), check whether the non-immune metadata's real author-provided `cluster` column (`Tu01`-`Tu11` tumor clusters, matching the publication's stated 11 tumor-cell clusters exactly; see `file_inventory.tsv`) can be joined and used directly as source-defined tumor-cell labels — this would be more informative than a fresh single-gene proxy. Reviewer's stated 3-step order once the counts file is opened: (1) confirm cell-ID join between counts and metadata; (2) confirm the five `A_CLINICAL` targets exist in the gene index; (3) confirm the author tumor-cell annotation is directly reusable — only then compute prevalence. Also keep the metadata's 7-vs-6-patient discrepancy explicitly unresolved; do not silently treat the immune file as 7 independent patients before reconciling it.
4. Optional lower-priority follow-ups from the Phase 1 verification pass: query the PRIDE API file listing for `PXD055821`/`PXD022613`/`CSPA_PXD000589`; fetch exact GEO `filelist.txt` for `GSE196576`/`GSE235917`; resolve `CPTAC_COAD`'s exact PDC study ID via its GraphQL/REST API instead of the unfetchable SPA UI.
5. Not next (PR #72 round 2's own reviewer note): extending `ADC_TARGET_SEED_UNIVERSE.tsv` coverage into ADCdb's `B_PRECLINICAL_ADC` / `C_ANTIBODY_OR_BIOLOGY_ONLY` tiers (the ~300-antigen pool not yet reviewed) — the 23-target `A_CLINICAL` slice is enough to prove out whether Modules B–F can produce a real KILL/HOLD/SHORTLIST pruning before widening intake.
6. Not next: reactivating any `SUPPLEMENT_FROZEN` dataset (DepMap, HTAN, CRLM-NMP, Perturb-seq, …) without a named target-specific uncertainty.

## Review history

- **PR #70, round 1** — Initial pivot commit. Web-ChatGPT review (`CRC临床适应症地图`, `Biotech ideas` project): `REQUEST_CHANGES`. Five blockers: (1) target-first existed only in prose, no `target_id` data model; (2) two conflicting dataset-priority sources (`module_classification.tsv` vs `datasets.tsv.priority`) with no schema/vocabulary on the new one; (3) this status file and `knowledge/README.md` still presented the old Phase 2 state as current; (4) `qc_gse178318.py` / `apply_gse178318_qc.py` / `audit_hpa_target_window.py` still carried the old Fig1 marker panel by default, and several module READMEs contradicted canonical registry status; (5) `.gitignore` dropped the legacy `phase2/03_data/raw` etc. patterns, an unnecessary data-leak risk for other checkouts. Three non-blocking suggestions: reclassify `CSPA_PXD000589` out of Module B, register a separate HPA cancer/CRC layer for Module D, flag the PDO's 14-marker mIHC coverage limit.
- **PR #70, round 2** — All five round-1 blockers and all three suggestions addressed. Web-ChatGPT review: `REQUEST_CHANGES` again, but explicitly confirmed the core pivot direction (target-first, old-track archival, canonical execution priority, external Module A reference, script cleanup) is now real, not surface patching. Five new items, all narrower/contract-level rather than architectural: (1) `target_evidence.tsv` didn't express target × patient-territory × evidence-directness — same target's evidence across different mCRC populations could get merged into one dossier, and RNA/protein/surface evidence could only be told apart by claim text; (2) Module C conflated `persistence` with plain `clinical_endpoint_context` (first-line/pretreatment response-association cohorts read as if they proved post-treatment persistence), and `activation_context=context_specific` had no machine-readable territory; (3) Module B's README implied Module D calibration alone could upgrade `RNA_high` to a surface-density claim, which Module D's own caveats already reject; (4) Module A's `target_seed.tsv` had no machine-readable admission rule for whether a target is even in active repurposing search; (5) a batch of consistency issues (candidate-count arithmetic, a stale `twelve_pivot_candidates` config key, an overclaimed file-inventory sentence, a stale Module A/B reason string, and a stale PR description).
- **PR #70, round 3** — All five round-2 items and all consistency issues addressed. Web-ChatGPT independently spot-checked GSE274551/GSE225857/GSE84267/PXD055821/PXD022613 against their official source records and found no problem with the candidate set itself; still `REQUEST_CHANGES`, but on two narrow scientific-semantics blockers plus one wording fix: (1) `GSE274551` (and, on inspection, `GSE178318`/`GSE225857`/`GSE294385`) was labeled `persistence`, but per official GEO design it/they are single-timepoint biopsies in refractory/treated tissue, not a paired pre/post-treatment measurement — only `GSE84267` actually is one; (2) `derisking_tier=B_PRECLINICAL_ADC`'s definition included "antibody-internalization evidence exists" on its own, which under-derisks a target relative to this Atlas's repurposing-first premise; (3) "Modules B and E (the two `CORE_ACTIVE`/`every_target` modules)" is factually wrong (C and D also have such rows) — should read as an intentional first-pass slice, not an exhaustive list. Reviewer's own stated bar: fixing these three closes the review with `APPROVE`, no further scope expansion.
- **PR #70, round 4** — All three round-3 items addressed: Module C's `persistence` axis retired and split into `longitudinal_persistence` / `refractory_or_treated_presence`; `B_PRECLINICAL_ADC` now requires a real preclinical ADC construct, not antibody-internalization alone; the Next-handoff wording corrected (see "Completed this pivot" item 9 above). Web-ChatGPT final review, head `b00d7ec`: **`APPROVE`**. No new blockers found; explicit note that this PR's job is the strategic pivot and execution contract, not starting real data analysis, and that job is now met. Merged.
- **PR #71, round 1** — Phase 1 source verification pass for the 13 pivot-added candidates (see "Phase 1 verification pass" above). Web-ChatGPT review: `REQUEST_CHANGES` — the pass discovered real new facts but hadn't fully propagated them into canonical fields, and one dataset's registered role was scientifically wrong: (1) `GSE235917` was registered as tumor-cell-localization support, but its own verified manifest and official GEO record show it's PBMC/TIL (immune-cell) scRNA-seq; (2) `GSE225857`'s `treatment_annotation` was wrongly downgraded to `UNKNOWN` when GEO's own "Overall design" field already states it; (3) `CPTAC_COAD`'s publication-only verification was folded into a "13/13 complete" claim; plus `GSE196576` sample-count and `GSE5851` citation cleanups.
- **PR #71, round 2** — All five round-1 items addressed, including reclassifying `GSE235917` to `SUPPLEMENT_FROZEN`. Web-ChatGPT independently re-verified `GSE225857`, `GSE196576`, and `GSE5851` against official sources and confirmed all landed correctly; still `REQUEST_CHANGES` on two remaining canonical-metadata inconsistencies: `GSE196576.primary_or_metastatic` was still oversimplified as `PRIMARY` (GEO's design is mixed primary/metastatic/unknown-origin), and `CPTAC_COAD`'s `module_classification.tsv` reason still said "source-verified."
- **PR #71, round 3** — Both remaining items fixed. Web-ChatGPT final review, head `2a93817`: **`APPROVE`**, with one non-blocking wording note (fixed in the same commit before merge). Explicit next step: stop polishing Phase 1 metadata, move to Module A's `ADC_TARGET_SEED_UNIVERSE.tsv`. Merged.
- **PR #72, round 1** — Initial Module A build, head `01aa76b`. Web-ChatGPT review: `REQUEST_CHANGES` — not a draft PR; and, more substantively, the generator was not fail-closed for a canonical artifact (hardcoded env var names instead of reading `config/external_sources.yaml`, silently warned-and-skipped on candidate/crossref reconciliation drift or a vanished antigen file), `human_adc_exposure_evidence` unconditionally `YES` (a clinical-stage upgrade, not documented exposure), and `adcdb_cancer_types_with_precedent`'s distillation had three live bugs (negative-context indication text counted as CRC-positive, unsafe `;` splitting shredding a cytogenetic-notation label, `cap` not actually bounding total shown terms). See "Module A seed universe build" above for the round-1→round-2 fix detail.
- **PR #72, round 2** — All four items fixed (see above); PR reverted to draft. Web-ChatGPT final review, head `a100198`: **`APPROVE`** — confirmed each item genuinely fixed, not reworded; explicit next step: do not extend Module A into ADCdb's ~300-antigen `B_PRECLINICAL_ADC`/`C_ANTIBODY_OR_BIOLOGY_ONLY` pool yet — take the first `target_id x indication_id` through Modules B and E instead. Marking the PR ready surfaced a separate, CI-only problem not part of the ChatGPT review: `.github/workflows/validate.yml`'s required check (`python3 -m unittest discover -s tests -p 'test_*.py'`) had been failing on `main` since PR #70 merged — `tests/test_github_scanners.py` hardcoded an expected `SOURCE_ONLY_CLOSURE_MATRIX.tsv` row count of `19` (the pre-pivot registry size), never updated when the registry grew to 32. Not caught earlier because the manual pre-merge validation sweep for PR #70/#71 never ran the actual `unittest discover` command CI uses. Fixed by comparing against the live registry row count instead of a hardcoded number, so it can't silently go stale again; `CONTRIBUTING.md` step 7 now lists this command explicitly. Merged.
- **PR #73, round 1** — First real Module B + E attempt, `tgt_ceacam5`, head `01aa76b`. Web-ChatGPT review: `REQUEST_CHANGES` — `TE001`/`TE002` used a different `indication_id` than Module B, splitting one target into two dossiers; HPA RNA file mislabeled "consensus" and the data lock wrongly claimed IHC was unavailable when this repository's own `source_manifest.tsv` already recorded it downloaded; two claims drifted into a proxy upgrade; Module B's blocker description contradicted this repository's own `GSE178318/source_manifest.tsv`, which records its matrix as already downloaded and checksum-verified.
- **PR #73, round 2** — All four items addressed, head `32c4c1c`: unified `indication_id`; real HPA cell-type-resolved IHC evidence added (`TE003`) after confirming the file is genuinely present locally; HPA RNA product renamed correctly; GTEx manifest backfilled; proxy-upgraded claims reworded; Module B data lock corrected (`GSE178318` data present, cell-type annotation is the real gap; `GSE225857` genuinely has no local data); `prior_therapy` ontology field loosened to match source support. Web-ChatGPT review: `REQUEST_CHANGES` — one remaining item: `TE003` was `CALIBRATED_PROXY` with no actual calibration step behind it (should be `UNCALIBRATED_PROXY`, `measurement_layer=IHC` already distinguishes it from RNA); "resolves" language still too strong in a few places; PR description still stale (Round 1 text).
- **PR #73, round 3** — Both remaining items fixed, head `7474004`: `TE003` corrected to `UNCALIBRATED_PROXY`; "resolves/confines" language replaced everywhere with "does not corroborate ... lowers but does not eliminate the concern"; PR description synced. Web-ChatGPT final review: **`APPROVE`** — confirmed no fix-vs-document drift across `target_evidence.tsv`, module docs, `PROJECT_STATUS.md`, and `knowledge/README.md`; explicitly recommended closing this one vertical slice (finish `GSE178318`'s malignant/epithelial cell annotation → `CEACAM5` Module B prevalence) before batch-running the other four CRC-precedented targets. Merged.
- **PR #74, round 1** — First `GSE178318` cell-typing attempt, head `cf2b6a6`. Web-ChatGPT review: `REQUEST_CHANGES` — independently fetched `GSE178318`'s own publication (also caught a non-existent DOI this repository's own `source_manifest.tsv` had recorded) and found the method fell below the dataset's own published standard: no QC filtering (paper: 111,292/140,281 cells via explicit thresholds); a scoring-math bug where every category divided by the same total-UMI denominator, cancelling it out of the argmax comparison and structurally favoring categories with more marker genes; an epithelial marker panel that risked misclassifying normal hepatic epithelium in liver-metastasis samples; treatment-naive patients folded into a treatment-defined `indication_id` with only a notes-field caveat, which "cannot fix wrong cohort inclusion." Offered two paths: reproduce the paper's QC/EPCAM/InferCNV pipeline, or explicitly downgrade to "epithelial-proxy screen, not malignancy-confirmed."
- **PR #74, round 2** — Took the recommended parts of both paths: real QC using the paper's own thresholds (123,330/140,281 pass), EPCAM-alone epithelial identification (matching the paper's method), fixed marker-average scoring, treated (`TE004`)/treatment-naive (`TE005`, re-keyed to `indication_id=mcrc_liver_metastasis`) split into separate rows, DOI corrected, reframed everywhere as an epithelial-proxy screen (not malignancy-confirmed, not a closed vertical slice), head `2ab59fa`. Web-ChatGPT final review: **`APPROVE`** — confirmed all four items genuinely fixed, and noted the corrected method's own output (few epithelial-proxy cells in `COL17`/`COL18`, more in `COL15`) is directionally consistent with the source paper's reported EPC distribution among treated patients — a real corroborating signal. Two small non-blocking wording/maintenance notes (QC description overstated as verbatim paper thresholds; unused reference-only marker rows not labeled as such) applied before merge. Explicit next step if continuing this slice: InferCNV/malignancy confirmation, not further proxy-classifier tuning. Merged.
- **PR #75, round 1** — First CNV-lite malignancy-confirmation attempt, head `2fdb7d8`. Web-ChatGPT review: `REQUEST_CHANGES` — same class of config bug as PR #72 round 1 (`resolve_hgnc_path()` hardcoded the `HGNC_GENE_ID_MAPPING_PATH` env var name instead of reading it out of `config/external_sources.yaml`'s `module_b_gene_position_reference` block); no checksum lock on the external HGNC reference file; the write-up's "reference" comparison numbers mixed the fit half (defines per-arm mean/SD) and the full un-split population inconsistently, and mislabeled the fit half's own 99th percentile as "the threshold, by construction" when the threshold actually comes from the held-out half; "~1% of epithelial-proxy cells clear the threshold" was reported without the reference's own exceedance rate at that same threshold, which is the null expectation by construction and needed for the reader to judge enrichment; the output file/console text used "confirmed" language that contradicted the PR's own "not confirmatory" conclusion; the "no cross-cell smoothing" explanation for the method's underpowering mischaracterized real InferCNV's actual primary mechanism (within-cell, genome-ordered smoothing, not cross-cell clustering); one numeric typo (25,390 vs. the canonical 25,376 genes mapped).
- **PR #75, round 2** — All seven round-1 items fixed (config actually parsed, HGNC file checksum-locked, held-out-half bookkeeping consistent, exact exceedance fractions + 1.23x enrichment ratio reported, output renamed to `tgt_ceacam5_cnv_lite_attempt.tsv`, InferCNV-mechanics explanation corrected, 25,376 typo fixed), head `97c6aa7`. Web-ChatGPT review: `REQUEST_CHANGES` — confirmed all seven round-1 fixes genuinely landed, but found a new blocker: `TE006`/`EV014` are keyed to `indication_id=mcrc_preop_chemotherapy_crlm` (the treated-cohort territory `TE004` also uses), but the script's reference and epithelial-proxy populations were not actually restricted to the 3 treated patients (`COL15`/`COL17`/`COL18`) — both silently pooled all 6 `GSE178318` patients, so the reported n=9,973 epithelial-proxy population was 82% treatment-naive cells (8,187 of 9,973, per the already-locked `TE004`/`TE005` per-sample counts) reported under a treated-only dossier — the same class of cohort-mismatch bug PR #74 round 1 caught for the epithelial-proxy screen itself. Minimal fix requested: restrict both populations to the treated cohort for the canonical `TE006` statistic (pooled six-patient numbers may stay as a non-canonical method diagnostic), plus sync the GitHub PR title/description (still round-1 text on GitHub, contradicting the corrected round-2 head). No new methodological scope requested — reviewer explicitly said accept whatever the treated-only numbers turn out to be.
- **PR #75, round 3** — Cohort mismatch fixed at the execution layer: `build_populations()`/`score_population()` helpers split the script into a canonical treated-only run (both reference and epithelial-proxy populations restricted to `COL15`/`COL17`/`COL18`) and a pooled six-patient diagnostic (non-canonical, stderr-only). Re-run on LOCAL: treated-only reference n=38,003 (held-out half n=19,002), threshold=12.54, epithelial-proxy n=1,786, median 1.32 vs. held-out reference median 0.63 (~2.1x), CNV_HIGH 36/1,786 (2.02%) vs. held-out reference's own 190/19,002 (1.00%), enrichment ratio 2.02x. Propagated to the analysis contract, `TE006`/`EV014`, module docs, `PROJECT_STATUS.md`; GitHub PR title/description synced ("ambiguous, not confirmatory"), head `3b9cfde`. Web-ChatGPT final review: **`APPROVE`** — confirmed the mismatch was fixed at the execution layer, not just documentation; `indication_id` now matches the actual computed population; noted approvingly that the evidence level was not upgraded just because the treated-only enrichment (2.02x) looked stronger than the earlier mistaken pooled number (1.23x) — still `UNCALIBRATED_PROXY`/`EXPLORATORY_UNDERPOWERED`/`LOW`. One non-blocking note (not worth a controlled-vocabulary redesign to block on): `EXPLORATORY_UNDERPOWERED` doesn't fully capture that the ambiguity is resolution + lineage confounding, not just statistical power. Explicit next-step recommendation: stop iterating CNV-lite further (risk of local overfitting to this one 6-patient cohort); prioritize unblocking `GSE225857` (a second, independent, treatment-exposed CRLM cohort) over a CNV-lite v2/v3. Merged.
- **PR #76, round 1** — First Module B + E pass for `ERBB2`/`F3`/`NECTIN4`/`TACSTD2`, head `651ae72`. Web-ChatGPT review: `REQUEST_CHANGES` — accepted the breadth-first scope decision itself (explicit user direction supersedes PR #75's earlier "don't batch-run" recommendation) and Module B's four screens, but found three Module E issues: `F3`'s IHC row (`TE014`) miscoded `evidence_directness=UNCALIBRATED_PROXY` despite its own `structured_value` recording `usable_data=NONE` (should be `UNKNOWN` — "no evidence must not be coded as weak evidence"); several canonical claims asserted uncited external clinical-toxicity/biology causality (ERBB2 "reproduces a known, real liability" of clinical anti-HER2 ADCs; similar for `NECTIN4`/`TACSTD2`/`F3`), one of them (TACSTD2/sacituzumab govitecan) also factually wrong (its urothelial-cancer indication was voluntarily withdrawn per FDA's 2024 letter; actual boxed-warning toxicities are neutropenia/diarrhea, not skin); two internal contradictions (a false "TACSTD2 is the only target with IHC-High" claim — `CEACAM5` already has 12 from PR #73 — and `NECTIN4`/`TACSTD2` both claiming "no GI tissue scored High/Medium" while listing Esophagus, GI tract, as Medium in the same sentence).
- **PR #76, round 2** — All round-1 items fixed: `TE014` → `UNKNOWN`; causal clinical-toxicity claims removed from `ERBB2`/`NECTIN4`/`TACSTD2`; TACSTD2's factual error corrected; both internal contradictions fixed; head `bd7eff2`. Web-ChatGPT review: `REQUEST_CHANGES` — confirmed most fixes landed, but found residual uncited external claims (ERBB2's IHC row still called cardiomyocytes/alveolar cells "clinically-monitored toxicity domains for approved anti-HER2 ADCs"; `F3` still said its RNA pattern "matches known F3 biology"; all four `question.md` files still asserted external drug/clinical facts even after TACSTD2's factual correction), a literal self-contradiction in TACSTD2's RNA claim ("skin is the single highest, but esophagus is higher still"), and a stale GitHub PR description. Explicitly scoped the fix: no data re-run, no Module B changes.
- **PR #76, round 3** — Stripped the remaining uncited external clinical/biology claims from `ERBB2`/`F3`'s canonical evidence; rewrote all four `question.md` files to reference Module A (`ADC_TARGET_SEED_UNIVERSE.tsv`, already source-verified) instead of restating drug/clinical facts in a Module E document; fixed the TACSTD2 wording self-contradiction; synced GitHub PR description, head `491794f`. Web-ChatGPT final review: **`APPROVE`** — confirmed all three round-2 items genuinely fixed: `F3`/`TE014` correctly `UNKNOWN` with `usable_data=NONE` preserved ("no evidence" no longer disguised as a weak proxy); ERBB2's IHC row reports only the HPA finding itself; all four `question.md` files properly defer to Module A; TACSTD2's RNA claim now logically consistent. One non-blocking wording note (bulk RNA alone can't itself prove an epithelial-cell source, though the same dossier's IHC row does and is cross-referenced) — not worth a further round. Explicit next-step recommendation: all five CRC-precedented targets now have comparable Module B+E screening evidence; move to horizontal decision/pruning (comparing which targets are near KILL/HOLD/worth advancing) rather than continuing to add same-type evidence per target. Merged.
- **PR #77, round 1** — First horizontal comparison across all five targets, head `40570d4`. Web-ChatGPT review: `REQUEST_CHANGES` — clean scope (new report only, no evidence data touched), but the substance pushed "structurally parallel evidence" into "quantitatively rankable": (1) Module B's cross-target prevalence ranking was uncalibrated across the five different genes (transcript abundance/dropout propensity/dynamic range differ per gene — a higher RNA detection fraction is not license for a "stronger/weaker biological prevalence" claim); (2) Module E's HPA IHC High/Medium levels are per-target/antibody categorical calls, not a cross-target intensity scale — the document's own later disclaimer ("Module E cannot establish accessible antigen density") directly contradicted its earlier "highest-intensity"/"risk-benefit least favorable" language; (3) a conceptual error treating `CEACAM5`'s normal-tissue signal being anatomically concentrated in the indication's own organ as resolving normal-tissue risk (normal colon/rectum is still normal tissue). Recommended reframing from "target pruning lean" to "evidence-based next-step prioritization."
- **PR #77, round 2** — Retitled both axes to make the uncalibrated-comparison limits explicit, removed all decision/ranking language, corrected `CEACAM5`'s framing to "pattern is anatomically concentrated, safety implication remains unresolved," reframed the whole document as next-uncertainty prioritization, head `aa3a140`. Web-ChatGPT review: `REQUEST_CHANGES` — confirmed the core restructuring correct, but the newly-introduced "only safe cross-target metric" (a count of distinct IHC-positive tissues) was itself miscounted: `TACSTD2` was called broadest, but `ERBB2`'s own table lists more distinct tissues (13 vs. 9). Also: stale filename (`TARGET_PRUNING_COMPARISON.md`, a semantic trap after the reframing) and "comparable" wording that could be misread as claiming calibration.
- **PR #77, round 3** — Recounted distinct HPA tissues with >=1 High/Medium row using a precisely-defined rule (`ERBB2` 13, `TACSTD2` 9, `CEACAM5` 6, `NECTIN4` 6, `F3` unmeasured); renamed the file to `TARGET_EVIDENCE_PATTERN_COMPARISON.md`; reworded "comparable" to "structurally parallel"; removed an unsupported "lowest-cost" claim, head `400be1d`. Web-ChatGPT review: `REQUEST_CHANGES` — confirmed all three items fixed, but caught a fresh, narrow contradiction the tissue-count fix itself introduced: the prose said `CEACAM5`'s 6-tissue footprint "sits entirely inside colorectal/appendix tissue," directly contradicted by the same table two lines above it, which lists Esophagus/Oral mucosa/Stomach among `CEACAM5`'s 6 tissues.
- **PR #77, round 4** — Split the sentence into two precise claims: `CEACAM5`'s High-level IHC calls are restricted to Appendix/Colon/Rectum, while its full High+Medium footprint also includes Esophagus/Oral mucosa/Stomach ("GI/oral-mucosal concentrated," not "entirely colorectal/appendix"), head `8be320b`. Web-ChatGPT final review: **`APPROVE`** — confirmed every evidence boundary established across all four rounds still holds (uncalibrated per-gene RNA detection, no cross-antibody IHC intensity scale, internally-consistent tissue-breadth count, `CEACAM5`'s anatomic concentration not misread as safety, `F3` still a genuine `UNKNOWN`, final output is next-uncertainty prioritization not `KILL`/`HOLD`/`SHORTLIST`). Explicit takeaway: the report's real value is turning "what's still missing" into a target-specific next-acquisition strategy, not a ranking. Merged.
- **PR #78, round 1** — `GSE225857` CNSA access-terms review, head `46ca3d5`. Web-ChatGPT review: `REQUEST_CHANGES` — independently re-verified `GSE225857`'s own GEO record and found the conclusion "extending Module B requires a DAC application" overclaimed (GSM7058754/GSM7058755 publicly provide downloadable count matrices + per-cell metadata, no access request needed — a real, unverified, materially cheaper alternative); an unsupported eligibility inference ("no Chinese-institution affiliation on record here") not stated by the CNGBdb policy text; `CNP0002540`/`CNP0003321` (two different-modality accessions) combined into one under-auditable provenance row.
- **PR #78, round 2** — Independently re-verified the reviewer's GEO findings before fixing; separated the confirmed CNSA `CONTROLLED_ACCESS` fact from the unverified "Module B needs that route" claim (rewritten `UNKNOWN`, correct cheaper next step recorded); removed the unsupported eligibility inference; split `CNP0002540`/`CNP0003321` into two provenance rows, head `408fb36`. Web-ChatGPT review: `REQUEST_CHANGES` — confirmed all three round-1 items fixed, but found the fix itself still called GEO's public GSM7058754/GSM7058755 files a "raw data"/"raw UMI count matrix" route, when GEO's own pages also state "Raw data not provided for this record" — these are processed-data files whose values are raw UMI counts, not raw sequencing reads. Requested consistent "processed count-matrix route" (GEO) vs. "raw sequencing-data route" (CNSA) terminology and a narrower PR title.
- **PR #78, round 3** — Fixed the raw-vs-processed terminology throughout `source_manifest.tsv`, `no_file_inventory_disposition.tsv`, `PROJECT_STATUS.md`, `knowledge/README.md`, `data_lock/tgt_ceacam5.md`; narrowed the PR title to "GSE225857 CNSA raw-data access review -- CONTROLLED_ACCESS; public GEO processed route remains open", head `c17a0e3`. Web-ChatGPT final review: **`APPROVE`** — confirmed the provenance boundary is clean: GEO is a public processed count-matrix + per-cell metadata route, CNSA is the raw sequencing-data route under `CONTROLLED_ACCESS`; CNSA being controlled-access does not by itself mean `GSE225857` is unusable for Module B, and whether the public GEO route suffices stays explicitly `UNVERIFIED`. Explicit next step: a small GEO-metadata sufficiency check (open the two public `*_meta.txt.gz` files and check for patient/site/treatment labels) before ever considering a DAC application. Merged.
- **PR #79, round 1** — GSE225857 public GEO metadata sufficiency check, head `ed424f8`. Actually downloaded and column-inspected `GSM7058754_immune_meta.txt.gz`/`GSM7058755_non_immune_meta.txt.gz` (real SHA256, gzip-integrity-verified); confirmed real patient/site resolution (`patients`, `organs`/`samples`, `patients_organ` columns; site codes independently cross-checked against the source publication, Wang et al. *Sci Adv* 2023, PMID 37327339; row counts match exactly). Recorded the 7-vs-6-patient discrepancy honestly rather than resolving or hiding it. Transitioned `GSE225857` from `no_file_inventory_disposition.tsv` to a real `file_inventory.tsv`, matching the file-inventory-vs-disposition binary convention every one of the registry's 32 datasets already follows. Web-ChatGPT final review: **`APPROVE`** — clean scope, no biological analysis smuggled in; independently confirmed the external-source framing; endorsed the inventory transition. Two non-blocking guardrails, folded in pre-merge (head `d31a2ad`): keep the 7-vs-6-patient discrepancy unresolved; narrow the next step to downloading only `GSM7058755_non_immune_counts.txt.gz` first and checking whether the metadata's `cluster` column already carries reusable author-provided tumor-cell labels — checked at zero additional cost, confirmed yes (11 `Tu01`-`Tu11` clusters, exact match to the publication's stated count). Merged.

## 权威项目记录

- [README.md](../README.md)：仓库目标、范围和 repository map
- [ADC_ATLAS_DATASET_CONTRACT.md](../ADC_ATLAS_DATASET_CONTRACT.md)：Module A-F 数据契约
- [modules/README.md](../modules/README.md)：模块工作目录索引
- [knowledge/README.md](../knowledge/README.md)：target-first evidence chain 和 schema
- [DATASET_REVIEW.md](DATASET_REVIEW.md)：Phase 1 候选、数据缺口和 stop condition
- [PHASE1_REVIEW_CHECKLIST.md](PHASE1_REVIEW_CHECKLIST.md)：逐候选 admission gate
- [PR_HISTORY.md](PR_HISTORY.md)：PR、网页版 ChatGPT 审核和合并记录
- [CHATGPT_CRC_CLINICAL_INDICATION_MAP_FEEDBACK.md](CHATGPT_CRC_CLINICAL_INDICATION_MAP_FEEDBACK.md)：外部审核反馈归档
- [updates/REVIEW_CHECKLIST.md](updates/REVIEW_CHECKLIST.md)：上游 pinned-target 漂移后的人工复核流程
- [P2_SAMPLE_METADATA.md](P2_SAMPLE_METADATA.md)：GSE178318 sample-level treatment/pairing reconciliation
- [PXD038149_SAMPLE_METADATA_GATE.tsv](PXD038149_SAMPLE_METADATA_GATE.tsv)：PXD038149 workbook staging and sample-metadata gate
- [SOURCE_ONLY_FINAL_AUDIT.tsv](SOURCE_ONLY_FINAL_AUDIT.tsv)：无生物数据边界审计结果
- `DATA/registry/*/no_file_inventory_disposition.tsv`：显式不主张 file-level inventory 的 source-only 处置
- [../archive/phase2_fetal_state_track_v1/PROJECT_STATUS_LEGACY_2026-08-11.md](../archive/phase2_fetal_state_track_v1/PROJECT_STATUS_LEGACY_2026-08-11.md)：pre-pivot status, historical only
