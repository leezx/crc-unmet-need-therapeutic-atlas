# Knowledge/review layer — source-only skeleton

This directory links clinical-indication ontology nodes to registry datasets without storing biological measurements or target conclusions.

**Target-first, as of 2026-08-21** (revised after web-ChatGPT review of PR #70, `REQUEST_CHANGES` items 1 and 3). The evidence chain here previously read `patient population → malignant cell state → surface target → functional dependency/payload vulnerability → normal-tissue therapeutic window`, which treats an unsupervised malignant-cell-state discovery step as a required intermediary between patient population and target. That framing is the old, now-archived Phase 2 fetal-state/plasticity direction (see `../archive/phase2_fetal_state_track_v1/ARCHIVE_NOTE.md`) and has been explicitly rejected as a required step. The current chain is:

`ADCdb-derisked target (Module A, external; gated by `derisking_tier`/`repurposing_status`) → mCRC evidence per axis and per territory (Modules B–F: prevalence, refractory_or_treated_presence, longitudinal_persistence, clinical_endpoint_context, protein/endpoint, normal-tissue risk, delivery proof, population proof) → KILL / HOLD / SHORTLIST decision for that target × indication → activate supplementary/mechanistic evidence (e.g. DepMap, spatial) only for a named residual uncertainty on an already-shortlisted target`

Malignant-cell state can still appear as one *input* to the prevalence/presence axes (e.g. "is this candidate detected in malignant cells vs adjacent normal") — it is no longer a mandatory discovery stage that has to run before a target exists.

**Two-table evidence model** (revised 2026-08-21, round 3 of the same PR #70 review). `schemas/evidence.tsv` and `schemas/indication_evidence_links.tsv` stay the **source/provenance object** layer — what a dataset's registry metadata plausibly indexes, `SOURCE_INDEXED_NOT_ANALYZED` until reviewed. Both now carry `target_id` (added round 2). The **canonical, target-level interpreted output** of Modules B–F is `schemas/target_evidence.tsv`, which does not just carry `target_id` but also:

- `indication_id`: the specific defined mCRC population/territory this finding applies to. The unit of a target dossier is `target_id x indication_id`, never a bare target — the same target can be `SHORTLIST` in one territory and `HOLD` in another.
- `measurement_layer` and `evidence_directness` (`DIRECT` / `CALIBRATED_PROXY` / `UNCALIBRATED_PROXY` / `UNKNOWN`): RNA, whole-tissue protein, IHC, surfaceome-capture, and a real quantitative surface assay are different measurement layers with different directness, not distinguishable only from claim text.
- `source_evidence_id` (nullable): points back to the raw `evidence.tsv` row when one exists.

The first 8 `evidence.tsv` objects predate the pivot, are dataset-provenance-level (not target-specific), and keep `target_id = NA`. As of 2026-08-23, `evidence.tsv` gained 6 real target-specific rows (`EV009`-`EV014`) and `target_evidence.tsv` gained its first 6 real rows (`TE001`-`TE006`), all `tgt_ceacam5` (`CEACAM5`, one of the 23 targets in `ADC_TARGET_SEED_UNIVERSE.tsv`): Module E's normal-tissue screen (`TE001`/`TE002` bulk RNA, `TE003` HPA cell-type IHC — one dossier, `indication_id=mcrc_preop_chemotherapy_crlm`), Module B's `GSE178318` epithelial-proxy prevalence screen split by actual treatment status (`TE004` the 3 treated patients, same `indication_id`; `TE005` the 3 treatment-naive matched pairs, `indication_id=mcrc_liver_metastasis`, context evidence), and a CNV-based malignancy-confirmation attempt (`TE006`, `scripts/infercnv_lite_gse178318.py`, a chromosome-arm-level CNV score against a held-out half of a treated-cohort tumor-site-immune reference — both the reference and the epithelial-proxy population restricted to the same 3 treated patients as `TE004`) whose honest result is **ambiguous, not confirmatory** — a modest median CNV-score shift in epithelial-proxy cells (2.02% clearing the held-out reference's own threshold vs. 1.00% for the reference itself, a 2.02x enrichment), directionally consistent with malignancy but not attributable with this coarse method to CNV signal vs. lineage-driven expression differences vs. resolution loss, and too underpowered at the single-cell level to confirm it or support a reliable prevalence number. **`TE004`-`TE006` do not confirm malignancy** — the source publication (Cell Discovery 2021, DOI `10.1038/s41421-021-00312-y`) confirms it via its own fine-grained InferCNV, not reproduced here; Module B's malignant-cell prevalence question for this target remains open. All six rows are `evidence_directness=UNCALIBRATED_PROXY`. See `modules/module_e_normal_tissue_risk/question|data_lock|analysis_contracts/tgt_ceacam5.md`, `modules/module_b_mcrc_target_prevalence/question|data_lock|analysis_contracts/tgt_ceacam5.md` and `.../analysis_contracts/infercnv_lite_gse178318.md`, `scripts/extract_normal_tissue_rna.py`, `scripts/annotate_gse178318_cell_types.py`, and `scripts/infercnv_lite_gse178318.py`. `GSE225857` (this module's other `CORE_ACTIVE` dataset) remains genuinely blocked on CNSA access for its raw single-cell data — not force-filled, no row for it. CNSA access terms independently reviewed 2026-08-24 and confirmed the raw sequencing-data route a real blocker (`CONTROLLED_ACCESS`: Data Access Center application to the original submitter plus China Human Genetic Resources regulatory compliance, not just an unreviewed metadata pointer — see `DATA/registry/GSE225857/source_manifest.tsv`), but this does not by itself rule out a separate, cheaper route: GEO's own public per-GSM record provides downloadable *processed* count matrices (round-2 review correction: not a raw-sequencing-data route — GEO's own record states raw data is not provided, only processed data) + per-cell metadata whose patient/site/treatment resolution is unverified. See `schemas/target_seed.tsv` (Module A's `ADC_TARGET_SEED_UNIVERSE.tsv` input/admission contract) in the repository-root `schemas/` directory.

**2026-08-24**: same Module B + E pipeline run for the four other `ADC_TARGET_SEED_UNIVERSE.tsv` targets with ADC precedent — `tgt_erbb2` (`ERBB2`), `tgt_f3` (`F3`), `tgt_nectin4` (`NECTIN4`), `tgt_tacstd2` (`TACSTD2`) — applying the already-corrected method from `tgt_ceacam5` on the first pass. `evidence.tsv` gained 20 more target-specific rows (`EV015`-`EV034`), `target_evidence.tsv` gained 20 more rows (`TE007`-`TE026`, 5 per target: RNA-HPA/RNA-GTEx/IHC for Module E, treated/treatment-naive for Module B) — 19 `evidence_directness=UNCALIBRATED_PROXY`, one `UNKNOWN` (`TE014`, `F3`'s IHC row, fixed 2026-08-24 PR #76 round 1 review: no usable IHC data exists, so it must not be coded as a weak proxy). No CNV-based malignancy confirmation attempted for any of the four (per the `tgt_ceacam5` CNV-lite reviewer's own guidance not to repeat that engineering effort per-target before deciding whether a higher-power method is worth investing in) — all four remain epithelial-proxy screens, not malignancy-confirmed. Real protein-level findings, reported as HPA/GTEx expression only, with **no causal clinical-toxicity claim** (corrected 2026-08-24, PR #76 round 1 review — an earlier version of this section asserted several unsupported "matches known ADC toxicity" claims, one of them, about `TACSTD2`, factually wrong): `ERBB2`'s IHC scores cardiomyocytes and lung alveolar cells Medium; `NECTIN4` shows Medium skin IHC on top of high skin RNA; `TACSTD2` reaches IHC-High in skin (one of two targets in this repository with any IHC-High row, alongside `CEACAM5`'s 12 from PR #73) but also has broad non-colorectal epithelial expression beyond skin (esophagus RNA is even higher than skin's). One notable data gap: `F3` has no usable HPA IHC data (one uninformative row, `Reliability=Uncertain`) — its normal-tissue assessment rests on RNA alone. `NECTIN4`/`TACSTD2`'s low-signal compartments are colon/rectum/small-intestine specifically, not GI broadly (both show real Medium IHC in esophagus, upper GI). One notable dataset-alias finding: `GSE178318`'s own gene index carries `NECTIN4` under its prior HGNC symbol, `PVRL4` — `scripts/annotate_gse178318_cell_types.py --gene PVRL4` was the actual invocation, results reported under the canonical `NECTIN4` symbol throughout (see `modules/module_b_mcrc_target_prevalence/data_lock/tgt_nectin4.md`).

The initial ontology follows the architecture requirement that a CRC indication is defined by more than a cancer label: disease state, molecular context, treatment line/history and anatomy must remain explicit. That indication-level context is `target_evidence.tsv`'s `indication_id` field, not a separate discovery stage:

`indication_id (defined mCRC population/territory) x target_id x ADC decision axis (prevalence / refractory_or_treated_presence / longitudinal_persistence / clinical_endpoint_context / protein_endpoint / normal_tissue_risk / delivery_proof / population_proof) → measurement_layer + evidence_directness`

Current `indication_evidence_links.tsv` rows are `SOURCE_INDEXED_NOT_ANALYZED`. They indicate that a dataset is a plausible source for an evidence axis based on its registry metadata; they do not establish a biological claim, therapeutic window, target ranking or clinical efficacy.

Files:

- `schemas/clinical_indications.tsv`: seed indication ontology nodes.
- `schemas/evidence.tsv`: source-only evidence objects with claims, provenance, confidence, target_id and explicit review status (provenance layer, not the canonical Module B–F output).
- `schemas/indication_evidence_links.tsv`: source-level links from indication nodes to evidence objects, registry datasets and (now) target_id.
- `schemas/target_seed.tsv`: contract for Module A's external `ADC_TARGET_SEED_UNIVERSE.tsv` output, including the `derisking_tier`/`repurposing_status` admission gate (see `../config/external_sources.yaml`).
- `schemas/target_evidence.tsv`: canonical, target x indication-keyed interpreted-evidence contract for Module B–F output. This is where every real finding goes.

The seed source is the user-provided `Asset-Generation-OS-architecture.md`, especially the CRC clinical-indication ontology and evidence-chain sections. Any future claim must add a reviewed source span, evidence object and human-review status.

Current source-only evidence objects: 34 (`evidence.tsv`) — 8 pre-pivot, dataset-provenance-level (`target_id = NA`), plus 26 target-specific rows (`EV009`-`EV034`, 6 `tgt_ceacam5` + 5 each of `tgt_erbb2`/`tgt_f3`/`tgt_nectin4`/`tgt_tacstd2`). `target_evidence.tsv`: 26 rows (`TE001`-`TE026`) — `tgt_ceacam5` (`TE001`-`TE006`, see above), `tgt_erbb2` (`TE007`-`TE011`), `tgt_f3` (`TE012`-`TE016`), `tgt_nectin4` (`TE017`-`TE021`), `tgt_tacstd2` (`TE022`-`TE026`); each of the four newer targets follows the same 5-row pattern as `tgt_ceacam5`'s first five (Module E RNA-HPA/RNA-GTEx/IHC, Module B treated/treatment-naive), minus a `TE006`-equivalent CNV-lite row (not attempted for any of the four). 25 rows `evidence_directness=UNCALIBRATED_PROXY`, one `UNKNOWN` (`TE014`, `F3`'s IHC row — no usable HPA IHC data exists for this gene, so "no evidence" is coded `UNKNOWN`, not a weak proxy; fixed 2026-08-24, PR #76 round 1 review). None has earned `CALIBRATED_PROXY` or `DIRECT`. DepMap 26Q1 is represented only at release level; its CRC subset and dependency results remain unmaterialized, and per `DATA/registry/module_classification.tsv` it is `SUPPLEMENT_FROZEN` (activate only per-target, after shortlist, for a named mechanistic question).
